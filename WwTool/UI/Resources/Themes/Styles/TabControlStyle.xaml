<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:b="clr-namespace:WwTool.Common.Behaviors">

    <Style TargetType="{x:Type TabItem}">
        <Setter Property="FocusVisualStyle" Value="{StaticResource StandardFocusVisualStyle}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}" />
        <Setter Property="Margin" Value="3" />
        <Setter Property="Padding" Value="14,8" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Tag" Value="{StaticResource DefaultImage}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TabItem}">
                    <Border
                        x:Name="mainBorder"
                        Padding="{TemplateBinding Padding}"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{DynamicResource BorderBrush}"
                        BorderThickness="1"
                        CornerRadius="10"
                        SnapsToDevicePixels="True">
                        <Grid>
                            <StackPanel
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Orientation="Horizontal"
                                TextElement.Foreground="{TemplateBinding Foreground}">
                                <Image
                                    x:Name="headerIcon"
                                    Width="16"
                                    Height="16"
                                    Margin="0,0,7,0"
                                    Opacity="0.62"
                                    Source="{TemplateBinding Tag}" />
                                <ContentPresenter
                                    x:Name="contentPresenter"
                                    VerticalAlignment="Center"
                                    ContentSource="Header"
                                    RecognizesAccessKey="True" />
                            </StackPanel>
                        </Grid>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
                            <Setter TargetName="headerIcon" Property="Opacity" Value="0.85" />
                            <Setter TargetName="mainBorder" Property="Background" Value="{DynamicResource SurfaceElevatedBrush}" />
                            <Setter TargetName="mainBorder" Property="BorderBrush" Value="{DynamicResource BorderStrongBrush}" />
                        </Trigger>

                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Foreground" Value="{DynamicResource AccentBrush}" />
                            <Setter Property="FontWeight" Value="SemiBold" />
                            <Setter TargetName="headerIcon" Property="Opacity" Value="1" />
                            <Setter TargetName="mainBorder" Property="Background" Value="{DynamicResource AccentContainerBrush}" />
                            <Setter TargetName="mainBorder" Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                        </Trigger>

                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{DynamicResource TextMutedBrush}" />
                        </Trigger>

                        <DataTrigger Binding="{Binding Path=(b:ResponsiveTabHeaderBehavior.IsCompact), RelativeSource={RelativeSource AncestorType={x:Type TabControl}}}" Value="True">
                            <Setter TargetName="contentPresenter" Property="Visibility" Value="Collapsed" />
                            <Setter TargetName="headerIcon" Property="Margin" Value="0" />
                            <Setter TargetName="mainBorder" Property="Padding" Value="10,8" />
                        </DataTrigger>

                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding Path=(b:ResponsiveTabHeaderBehavior.IsCompact), RelativeSource={RelativeSource AncestorType={x:Type TabControl}}}" Value="True" />
                                <Condition Binding="{Binding IsMouseOver, RelativeSource={RelativeSource Self}}" Value="True" />
                            </MultiDataTrigger.Conditions>
                            <Setter TargetName="contentPresenter" Property="Visibility" Value="Visible" />
                            <Setter TargetName="headerIcon" Property="Margin" Value="0,0,7,0" />
                            <Setter TargetName="mainBorder" Property="Padding" Value="14,8" />
                        </MultiDataTrigger>

                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding Path=(b:ResponsiveTabHeaderBehavior.IsCompact), RelativeSource={RelativeSource AncestorType={x:Type TabControl}}}" Value="True" />
                                <Condition Binding="{Binding IsSelected, RelativeSource={RelativeSource Self}}" Value="True" />
                            </MultiDataTrigger.Conditions>
                            <Setter TargetName="contentPresenter" Property="Visibility" Value="Visible" />
                            <Setter TargetName="headerIcon" Property="Margin" Value="0,0,7,0" />
                            <Setter TargetName="mainBorder" Property="Padding" Value="14,8" />
                        </MultiDataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type TabControl}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Stretch" />
        <Setter Property="Padding" Value="0" />

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TabControl}">
                    <Grid KeyboardNavigation.TabNavigation="Local" SnapsToDevicePixels="True">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <Border
                            Grid.Row="0"
                            Margin="0,0,0,12"
                            Padding="4"
                            Background="{DynamicResource SurfaceMutedBrush}"
                            BorderBrush="{DynamicResource BorderBrush}"
                            BorderThickness="1"
                            CornerRadius="{DynamicResource CornerRadiusCard}">
                            <ScrollViewer
                                x:Name="PART_HeaderScrollViewer"
                                HorizontalScrollBarVisibility="Hidden"
                                PanningMode="HorizontalOnly"
                                VerticalScrollBarVisibility="Disabled">
                                <Border Padding="2,2,8,7" Background="Transparent">
                                    <StackPanel
                                        x:Name="headerPanel"
                                        Background="Transparent"
                                        IsItemsHost="True"
                                        KeyboardNavigation.TabNavigation="Once"
                                        Orientation="Horizontal"
                                        SnapsToDevicePixels="True"
                                        UseLayoutRounding="True" />
                                </Border>
                            </ScrollViewer>
                        </Border>

                        <Border Grid.Row="1" Padding="{TemplateBinding Padding}">

                            <ContentControl
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                Content="{Binding SelectedContent, RelativeSource={RelativeSource TemplatedParent}, NotifyOnTargetUpdated=True}"
                                ContentStringFormat="{Binding SelectedContentStringFormat, RelativeSource={RelativeSource TemplatedParent}}"
                                ContentTemplate="{Binding SelectedContentTemplate, RelativeSource={RelativeSource TemplatedParent}}"
                                ContentTemplateSelector="{Binding SelectedContentTemplateSelector, RelativeSource={RelativeSource TemplatedParent}}"
                                Focusable="False">

                                <ContentControl.RenderTransform>
                                    <TranslateTransform X="0" />
                                </ContentControl.RenderTransform>

                                <ContentControl.Style>
                                    <Style TargetType="ContentControl">
                                        <Style.Triggers>
                                            <EventTrigger RoutedEvent="Binding.TargetUpdated">
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <DoubleAnimation
                                                            Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)"
                                                            From="30"
                                                            To="0"
                                                            Duration="{StaticResource MotionPage}">
                                                            <DoubleAnimation.EasingFunction>
                                                                <CubicEase EasingMode="EaseOut" />
                                                            </DoubleAnimation.EasingFunction>
                                                        </DoubleAnimation>

                                                        <DoubleAnimation
                                                            Storyboard.TargetProperty="Opacity"
                                                            From="0"
                                                            To="1"
                                                            Duration="{StaticResource MotionNormal}" />
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </EventTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </ContentControl.Style>
                            </ContentControl>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>

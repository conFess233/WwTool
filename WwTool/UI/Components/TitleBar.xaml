<UserControl
    x:Class="WwTool.UI.Components.TitleBar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:gif="clr-namespace:XamlAnimatedGif;assembly=XamlAnimatedGif"
    xmlns:utils="clr-namespace:WwTool.Common.Utils"
    Height="32">

    <Border ClipToBounds="True" CornerRadius="12">
        <Grid Background="#01FFFFFF" MouseLeftButtonDown="Grid_MouseLeftButtonDown">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel
                Grid.Column="0"
                Margin="12,0,0,0"
                IsHitTestVisible="False"
                Orientation="Horizontal">
                <Image
                    Width="30"
                    Height="30"
                    Margin="0,0,8,0"
                    gif:AnimationBehavior.SourceUri="/UI/Resources/Images/Icon.gif"
                    Opacity="0.8" />
                <TextBlock
                    Margin="10,0,0,0"
                    FontSize="{StaticResource FontSizeSm}"
                    Style="{StaticResource TitleTextStyle}"
                    Text="{Binding Path=[Title_Toolbox], Source={x:Static utils:LanguageManager.Instance}}" />
            </StackPanel>

            <StackPanel Grid.Column="2" Orientation="Horizontal">
                <Button Click="BtnMinimize_Click" Style="{StaticResource SystemButtonStyle}">
                    <Path
                        Data="M 0,0 L 10,0"
                        SnapsToDevicePixels="True"
                        Stroke="{DynamicResource TextSubBrush}"
                        StrokeThickness="1" />
                </Button>
                <Button Click="BtnMaximize_Click" Style="{StaticResource SystemButtonStyle}">
                    <Path
                        x:Name="PathMaximize"
                        Data="M 0,0 L 10,0 L 10,10 L 0,10 Z"
                        SnapsToDevicePixels="True"
                        Stroke="{DynamicResource TextSubBrush}"
                        StrokeThickness="1" />
                </Button>
                <Button Click="BtnClose_Click">
                    <Button.Style>
                        <Style TargetType="{x:Type Button}">
                            <Setter Property="Background" Value="Transparent" />
                            <Setter Property="Width" Value="40" />
                            <Setter Property="Height" Value="28" />
                            <Setter Property="BorderThickness" Value="0" />
                            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type Button}">
                                        <Grid Background="Transparent">
                                            <Border
                                                x:Name="border"
                                                Margin="2"
                                                Padding="1"
                                                Background="{TemplateBinding Background}"
                                                CornerRadius="4"
                                                SnapsToDevicePixels="True" />
                                            <Border
                                                x:Name="hoverBorder"
                                                Margin="2"
                                                Padding="1"
                                                Background="{DynamicResource CancelButtonHoverBrush}"
                                                CornerRadius="4"
                                                Opacity="0"
                                                SnapsToDevicePixels="True" />
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                        </Grid>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Trigger.EnterActions>
                                                    <BeginStoryboard>
                                                        <Storyboard>
                                                            <DoubleAnimation
                                                                Storyboard.TargetName="hoverBorder"
                                                                Storyboard.TargetProperty="Opacity"
                                                                To="1"
                                                                Duration="0:0:0.2" />
                                                        </Storyboard>
                                                    </BeginStoryboard>
                                                </Trigger.EnterActions>
                                                <Trigger.ExitActions>
                                                    <BeginStoryboard>
                                                        <Storyboard>
                                                            <DoubleAnimation
                                                                Storyboard.TargetName="hoverBorder"
                                                                Storyboard.TargetProperty="Opacity"
                                                                To="0"
                                                                Duration="0:0:0.2" />
                                                        </Storyboard>
                                                    </BeginStoryboard>
                                                </Trigger.ExitActions>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                    <Path
                        Data="M 0,0 L 10,10 M 10,0 L 0,10"
                        SnapsToDevicePixels="True"
                        Stroke="{DynamicResource TextSubBrush}"
                        StrokeThickness="1" />
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>